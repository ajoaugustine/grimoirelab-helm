{{- if .Values.arthur.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "grimoirelab.fullname" . }}-arthur
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "grimoirelab.labels" . | nindent 4 }}
    app.kubernetes.io/component: arthur
spec:
  type: {{ .Values.arthur.service.type }}
  ports:
    - port: {{ .Values.arthur.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grimoirelab.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: arthur
{{- end }}
